compile main.cpp , and run a.out

When the prgram is running choose which algo to run.

Everything is outputted to the screen.

What being outputted are a subset of all the edges included in the graph.

choose 9 to quit.
